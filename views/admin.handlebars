<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Document</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css"
    integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous" />
  <link rel="stylesheet" href="/style/admin.css" />
</head>

<body>
  <main>
    <img src="./img/logo_blanco.png" alt="Logo" class="logo">
  </main>
  <div class="py-4">
    <h2>/// Administración ///</h2>
    <hr class="w-auto" />
    <div class="container">
      <div class="row justify-content-md-center">
        <table class="text-white col-md-8">
          <thead>
            <tr>
              <th scope="col">#</th>
              <th scope="col">Foto</th>
              <th scope="col">Nombre</th>
              <th scope="col">Años de experiencia</th>
              <th scope="col">Especialidad</th>
              <th scope="col">Estado</th>
            </tr>
          </thead>
          <tbody>
            {{#if skaters}}
            {{#each skaters}}
            <tr>
              <th scope="row">{{this.id}}</th>
              <td><img src="./img/{{this.foto}}.jpg" alt="imgen usuario" class="imgUser"></td>
              <td>{{this.nombre}}</td>
              <td>{{this.anos_experiencia}}</td>
              <td>{{this.especialidad}}</td>
              <td><input type="checkbox" id="estado{{this.id}}" {{#if this.estado}}checked{{/if}} /></td>
            </tr>
            {{/each}}
            {{/if}}
          </tbody>
        </table>
      </div>
    </div>
  </div>

<script type="text/javascript" src="./js/admin.js"></script>
</body>

</html>